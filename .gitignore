# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.npm
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
.pnp.*

# Environment files
.env
.env.*
!.env.example

# Build outputs
dist/
build/
out/
.turbo/
.cache/
*.tsbuildinfo

# NOTE: tokens/dist/ is INTENTIONALLY committed. It is the build output of
# the Style Dictionary pipeline, and the Next.js reference app under
# reference/ imports from it directly. Committing it keeps Vercel's default
# Next.js auto-detection working (no custom build chain, no cd .. tricks).
# ALWAYS run `npm run build:tokens` before committing token source changes,
# so the committed dist/ stays in sync with the JSON sources.
!tokens/dist/
!tokens/dist/**

# Tokens Studio import workspace — raw source, extracted files, migration
# script, and report. Only the migrated tokens/*.json files are committed.
tokens-studio-import/

# Next.js
.next/
next-env.d.ts

# Vercel
.vercel

# Testing
coverage/
*.lcov
.nyc_output

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Editors — VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace
.history/

# Editors — JetBrains
.idea/
*.iml
*.iws
*.ipr

# Editors — Sublime / Vim
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~

# Logs
logs/
*.log

# Temporary files
.tmp/
.temp/
